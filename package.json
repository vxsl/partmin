{
  "name": "partmin",
  "private": true,
  "license": "MIT",
  "scripts": {
    "server": "cd packages/server && yarn start",
    "server-dev": "cd packages/server && yarn dev"
  },
  "workspaces": [
    "packages/*"
  ]
}
